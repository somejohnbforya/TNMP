{
  "name": "tnmp-notifications",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.88.0"
  },
  "dependencies": {
    "@cf-wasm/resvg": "^0.3.4",
    "chess.js": "^1.4.0"
  }
}
